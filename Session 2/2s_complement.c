#include <stdio.h>

int main()
{
    int nr=-5;
    unsigned nr2=5;
    printf("The number is %d and in hexa 0x%x \n", nr,nr);
    printf("The number is %u and in hexa 0x%x", nr2,nr2);

    return 0;
}